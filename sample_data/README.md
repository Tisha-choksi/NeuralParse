# Sample Data

Mock business documents for testing the NeuralParse MVP.

Upload these files through the app to test:

- document classification
- field extraction
- validation rules
- human review routing
- analytics counts

Start with the invoice files because the current extractor already looks for invoice-style fields such as invoice number, vendor, GST, amount, date, and due date.

